(* project settings:
  {#include
    source = "$(project_root)source"
  #}
*)
program {#project_name#};

uses
  Gen2MP,
  GameUnit;

begin
  WriteLn('Hello G2MP');
  //Game := TGame.Create;
  //g2.Start;
  //Game.Free;
end.