{#g2af$(page_begin)
 {#page
  image = "$(page_file)"
  width = $(page_width)
  height = $(page_height)$(frame_begin)
  {#frame
   name = "$(frame_file)"
   pos_l = $(frame_l)
   pos_t = $(frame_t)
   width = $(frame_width)
   height = $(frame_height)
  #}$(frame_end)
 #}$(page_end)
#}